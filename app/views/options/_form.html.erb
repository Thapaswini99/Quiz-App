<%= form_with(model: option, local: true) do |form| %>
  <% if option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(option.errors.count, "error") %> prohibited this option from being saved:</h2>

      <ul>
      <% option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Option %>
    <%= form.text_field :Option, id: :option_Option %>
  </div>

  <div class="field">
    <%= form.label :flag %>
    <%= form.check_box :flag, id: :option_flag %>
  </div>

  <div class="field" style="display: none;">

    <%= form.collection_select :question_id, Question.where(:id => params[:question] ),:id,:Question, include_blank: false %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
