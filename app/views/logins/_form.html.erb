<%= form_with(model: signup, local: true) do |form| %>
  <% if login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(login.errors.count, "error") %> prohibited this login from being saved:</h2>

      <ul>
      <% login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :login_email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password, id: :login_password %>
  </div>



  <div class="field">
    <%= form.label :Admin %>
    <%= form.check_box :Admin, id: :login_Admin %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
